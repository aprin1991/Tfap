type IProps = {};
type IState = {
  name: string;
  storeName: string;
  email: string;
  phone: string;
  message: string;
  user: string;
  fullName: string;
  loading: boolean;
  showUserTypes?: boolean;
};

export type { IState, IProps };
